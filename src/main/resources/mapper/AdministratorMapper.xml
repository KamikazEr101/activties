<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xidian.activities.mapper.AdministratorMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.xidian.activities.entity.Administrator">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="username" property="username" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="real_name" property="realName" jdbcType="VARCHAR" />
        <result column="email" property="email" jdbcType="VARCHAR" />
        <result column="phone" property="phone" jdbcType="VARCHAR" />
        <result column="role_type" property="roleType" jdbcType="INTEGER" />
        <result column="account_status" property="accountStatus" jdbcType="INTEGER" />
        <result column="is_deleted" property="isDeleted" jdbcType="INTEGER" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="last_login_time" property="lastLoginTime" jdbcType="TIMESTAMP" />
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List"> id, username, password, real_name, email, phone, role_type,
        account_status, is_deleted, create_time, update_time, last_login_time </sql>

    <!-- 插入管理员 -->
    <insert id="insert" parameterType="com.xidian.activities.entity.Administrator"
        useGeneratedKeys="true" keyProperty="id"> INSERT INTO administrators ( username, password,
        real_name, email, phone, role_type, account_status, is_deleted, create_time, update_time,
        last_login_time ) VALUES ( #{username}, #{password}, #{realName}, #{email}, #{phone},
        #{roleType}, #{accountStatus}, #{isDeleted}, #{createTime}, #{updateTime}, #{lastLoginTime}
        ) </insert>

    <!-- 更新管理员 -->
    <update id="updateById" parameterType="com.xidian.activities.entity.Administrator"> UPDATE
        administrators <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="email != null">email
        = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="roleType != null">role_type
        = #{roleType},</if>
            <if test="accountStatus != null">account_status = #{accountStatus},</if>
            <if
                test="password != null">password = #{password},</if> update_time = NOW() </set>
        WHERE id = #{id} AND is_deleted = 0 </update>

    <!-- 软删除管理员 -->
    <update id="deleteById" parameterType="java.lang.Long"> UPDATE administrators SET is_deleted =
        1, update_time = NOW() WHERE id = #{id} AND is_deleted = 0 </update>

    <!-- 根据ID查询管理员 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap"> SELECT <include
            refid="Base_Column_List" /> FROM administrators WHERE id = #{id} AND is_deleted = 0 </select>

    <!-- 根据用户名查询管理员 -->
    <select id="selectByUsername" parameterType="java.lang.String" resultMap="BaseResultMap"> SELECT <include
            refid="Base_Column_List" /> FROM administrators WHERE username = #{username} AND
        is_deleted = 0 </select>

    <!-- 分页查询管理员 -->
    <select id="selectList" parameterType="com.xidian.activities.entity.Administrator"
        resultMap="BaseResultMap"> SELECT <include refid="Base_Column_List" /> FROM administrators
        WHERE is_deleted = 0 <if test="username != null and username != ''"> AND username LIKE
        CONCAT('%', #{username}, '%') </if>
        <if test="realName != null and realName != ''"> AND
        real_name LIKE CONCAT('%', #{realName}, '%') </if>
        <if test="roleType != null"> AND role_type
        = #{roleType} </if>
        <if test="accountStatus != null"> AND account_status = #{accountStatus} </if>
        <if
            test="email != null and email != ''"> AND email LIKE CONCAT('%', #{email}, '%') </if>
        ORDER BY create_time DESC </select>

    <!-- 更新最后登录时间 -->
    <update id="updateLastLoginTime" parameterType="java.lang.Long"> UPDATE administrators SET
        last_login_time = NOW(), update_time = NOW() WHERE id = #{id} AND is_deleted = 0 </update>

    <!-- 检查用户名是否存在 -->
    <select id="checkUsernameExists" resultType="java.lang.Integer"> SELECT COUNT(*) FROM
        administrators WHERE username = #{username} AND is_deleted = 0 <if test="excludeId != null">
        AND id != #{excludeId} </if>
    </select>

    <!-- 检查邮箱是否存在 -->
    <select id="checkEmailExists" resultType="java.lang.Integer"> SELECT COUNT(*) FROM
        administrators WHERE email = #{email} AND is_deleted = 0 <if test="excludeId != null"> AND
        id != #{excludeId} </if>
    </select>

    <!-- 修改密码 -->
    <update id="changePassword" parameterType="map"> UPDATE administrators SET password =
        #{newPassword}, updated_time = NOW() WHERE id = #{id} AND is_deleted = 0 </update>

    <!-- 查询管理员总数 -->
    <select id="countByCondition" parameterType="com.xidian.activities.entity.Administrator"
        resultType="java.lang.Long"> SELECT COUNT(*) FROM administrators WHERE is_deleted = 0 <if
            test="username != null and username != ''"> AND username LIKE CONCAT('%', #{username},
        '%') </if>
        <if test="realName != null and realName != ''"> AND real_name LIKE CONCAT('%',
        #{realName}, '%') </if>
        <if test="roleType != null"> AND role_type = #{roleType} </if>
        <if
            test="accountStatus != null"> AND account_status = #{accountStatus} </if>
        <if
            test="email != null and email != ''"> AND email LIKE CONCAT('%', #{email}, '%') </if>
    </select>

    <!-- 批量更新账户状态 -->
    <update id="batchUpdateStatus" parameterType="map"> UPDATE administrators SET account_status =
        #{status}, update_time = NOW() WHERE id IN <foreach item="id" collection="ids" open="("
            separator="," close=")"> #{id} </foreach> AND is_deleted = 0 </update>

</mapper>